import { FormatCustomSelectOptionListPipe } from './format-custom-select-option-list.pipe';

describe('FormatCustomSelectOptionListPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCustomSelectOptionListPipe();
    expect(pipe).toBeTruthy();
  });
});
