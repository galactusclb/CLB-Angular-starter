import { FileExtentionPipe } from './file-extention.pipe';

describe('FileExtentionPipe', () => {
  it('create an instance', () => {
    const pipe = new FileExtentionPipe();
    expect(pipe).toBeTruthy();
  });
});
